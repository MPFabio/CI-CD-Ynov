spring:
  datasource:
    url: jdbc:postgresql://app-db-1:5432/cookieclicker
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

# spring:
#   application:
#     name: Pause Clope
#   data:
#     rest:
#       base-path: /api/v1

#   datasource:
#     url: jdbc:postgresql://localhost:5432/cookieclicker
#     username: postgresjdbc:postgresql://localhost:5432/cookieclicker
#     password: password
#     driver-class-name: org.postgresql.Driver

#   jpa:
#     hibernate:
#       ddl-auto: update  # or 'none', 'create', 'create-drop', 'validate'
#     show-sql: true
#     properties:
#       hibernate:
#         format_sql: true
#     database-platform: org.hibernate.dialect.PostgreSQLDialect
